# ai meeting notes

macos app that detects zoom meetings, records audio, transcribes with whisper, and generates summaries.

## setup

```bash
npm install
npm start
```

requires ffmpeg (`brew install ffmpeg`) and an openai api key.

## build

```bash
npm run build
```
